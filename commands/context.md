---
name: wheee:context
description: Context Health pruefen (wheee context)
allowed-tools:
  - Bash
  - Read
---

<objective>
Run `wheee context` to check context health.
</objective>

<process>
1. Run `wheee context` in the project root.
2. Show the output to the user.
</process>
