---
name: wheee:perf
description: Performance-Regressionen pruefen (wheee perf)
allowed-tools:
  - Bash
  - Read
---

<objective>
Run `wheee perf` to check for performance regressions.
</objective>

<process>
1. Run `wheee perf` in the project root.
2. Show the results to the user.
</process>
