---
name: wheee:prototype
description: Prototyp aus Design erzeugen (wheee prototype)
allowed-tools:
  - Bash
  - Read
---

<objective>
Run `wheee prototype` to generate a prototype from the current design.
</objective>

<process>
1. Run `wheee prototype` (with arguments the user provided) in the project root.
2. Show the output to the user.
</process>
